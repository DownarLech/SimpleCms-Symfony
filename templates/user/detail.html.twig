{% extends 'base.html.twig' %}

{% block title %}Product details{% endblock %}

{% block body %}

    <h1>User ID: {{ user.id }}</h1>
    <h2>User Name: {{ user.username }}</h2>
    <h3>User Password :) : {{ user.password }}</h3>
    <h3>User Role: {{ user.userrole }}</h3>

    <a href="{{ path('user_delete' , {'userId' : user.id}) }}"><button>Delete</button></a>
    <a href="{{ path('user_update' , {'userId' : user.id}) }}"><button>Update</button></a>

{% endblock %}